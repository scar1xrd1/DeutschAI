import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const background = Color(0xFF1C1C1E);
  static const surface = Color(0xFF2C2C2E);
  static const surfaceLight = Color(0xFF3A3A3C);

  // Brand
  static const primary = Color(0xFF4F6BFF);
  static const primaryDark = Color(0xFF3E57D0);

  // Text
  static const textPrimary = Color(0xFFF2F2F7);
  static const textSecondary = Color(0xFFAEAEB2);
  static const hint = Color(0xFF8E8E93);

  // Status
  static const success = Color(0xFF30D158);
  static const warning = Color(0xFFFF9F0A);
  static const error = Color(0xFFFF453A);

  // Border
  static const border = Color(0xFF48484A);
}
